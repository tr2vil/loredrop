---
name: docker-up
description: Docker 컨테이너 시작 (데이터 안전)
disable-model-invocation: true
---

Docker 컨테이너를 시작한다.

1. `docker-compose up -d` 실행
2. `docker-compose ps` 로 상태 확인
3. 각 서비스(app, n8n)의 상태를 보고
